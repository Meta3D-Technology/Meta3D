import { meta3dState } from "./type";

// export declare function push(meta3dState): meta3dState
export let push = (meta3dState) => {
    return meta3dState
}

// export declare function undo(meta3dState): meta3dState
export let undo = (meta3dState) => {
    return meta3dState
}