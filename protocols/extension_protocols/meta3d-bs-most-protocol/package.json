{
    "name": "meta3d-bs-most-protocol",
    "version": "0.4.1",
    "publisher": "meta3d",
    "description": "",
    "author": "Wonder",
    "license": "MIT",
    "scripts": {
        "watch": "rescript clean && rescript build -w",
        "buildAll:ci": "rescript clean && rescript build",
        "meta3d:publish_dev": "yarn buildAll:ci && gulp publish",
        "meta3d:publish_pro": "yarn buildAll:ci && gulp publish"
    },
    "keywords": [],
    "devDependencies": {
        "rescript": "^9.1.4"
    }
}
