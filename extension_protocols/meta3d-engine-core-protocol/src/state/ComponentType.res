// @genType
type state

type transformState = state

type pbrMaterialState = state

type geometryState = state

type directionLightState = state

type arcballCameraControllerState = state

type basicCameraViewState = state

type perspectiveCameraProjectionState = state

// @genType
type component

// @genType
type config

type needDisposedComponents

type batchDisposeData

type cloneConfig