{
    "name": "meta3d-platform-publish",
    "version": "1.3.0",
    "main": "./src/Main.js",
    "scripts": {
        "watch": "tsc --watch",
        "bundle_dts": "gulp bundle_dts",
        "platform:publish_dev_minor": "gulp ci && gulp commit && gulp lerna_version_minor && gulp publish_local_minor_env",
        "platform:publish_pro_minor": "gulp ci && gulp commit && gulp lerna_version_minor && gulp publish_pro_minor_env",
        "platform:publish": "gulp publish_platform"
    },
    "devDependencies": {
        "@cloudbase/manager-node": "^4.2.8",
        "dts-bundle-generator": "^9.0.0",
        "gulp": "^4.0.2",
        "typescript": "^5.1.0"
    }
}
