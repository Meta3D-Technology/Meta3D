# TODO fix: actions: no updatevalue action + select event: no addcareer event
solution:
    import { actionName as addCareerFeatureActionName, state as addCareerFeatureState } from "meta3d-action-mod-career-add-careerfeature-protocol" should after: 
        import { actionName, state, uiData } from "meta3d-action-mod-career-updatevalue-protocol"


